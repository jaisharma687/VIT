#include <stdio.h> // This is a preprocessor directive that tells the compiler to include the standard input/output library

int main() // This is the main function where the program execution begins
{
    printf("Hello World"); // This is a function call that displays the message inside the quotation marks
    return 0; // This is the exit status of the program
}